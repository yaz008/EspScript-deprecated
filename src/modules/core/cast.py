def cast_to_bool(stdin: str) -> bool:
    return bool(stdin)

def cast_to_int(stdin: str) -> int:
    return int(stdin)

def cast_to_float(stdin: str) -> float:
    return float(stdin)

def cast_to_string(stdin: str) -> str:
    return str(stdin)