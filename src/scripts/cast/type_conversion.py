def cast_to_string(stdin: str, *args: str) -> int:
    return int(stdin)

def cast_to_int(stdin: str, *args: str) -> int:
    return int(stdin)

def cast_to_float(stdin: str, *args: str) -> int:
    return float(stdin)